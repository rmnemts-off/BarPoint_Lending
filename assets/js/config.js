/* ============================================================
   BARPOINT — конфигурация приёма заявок (ТЗ 9.1)
   ЗАПОЛНИТЬ ПЕРЕД ЗАПУСКОМ (раздел 12 ТЗ — данные заказчика):

   1. endpoint — URL серверной функции, принимающей POST JSON
      {name, phone, page, ts} (состав — ТЗ8 §4) и пересылающей заявку
      в Telegram (Bot API) и/или на email/CRM (Bitrix24, amoCRM).
   2. telegram — юзернейм для кнопки «Продублировать в Telegram»
      (сейчас — значение со слайда «Контакты»; уточнить, что это
      канал для приёма лидов, а не личный аккаунт менеджера).

   Пока endpoint не задан, форма честно предлагает пользователю
   отправить сформированную заявку напрямую в Telegram, а копию
   сохраняет в localStorage ("bp_leads") — ничего не теряется.
   ============================================================ */
window.BARPOINT_CONFIG = {
  endpoint: "",            // например: "https://barpoint.ru/api/lead"
  telegram: "INNA_ANTONYAN_WORK"
};
